const $=id=>document.getElementById(id);
const EUR=new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR',maximumFractionDigits:0});
const NUM=new Intl.NumberFormat('de-DE',{maximumFractionDigits:2});
const GR={BW:5,BY:3.5,BE:6,BB:6.5,HB:5.5,HH:5.5,HE:6,MV:6,NI:5,NW:6.5,RP:5,SL:6.5,SN:5.5,ST:5,SH:6.5,TH:5};
const SN={BW:'Baden-Württemberg',BY:'Bayern',BE:'Berlin',BB:'Brandenburg',HB:'Bremen',HH:'Hamburg',HE:'Hessen',MV:'Mecklenburg-Vorpommern',NI:'Niedersachsen',NW:'Nordrhein-Westfalen',RP:'Rheinland-Pfalz',SL:'Saarland',SN:'Sachsen',ST:'Sachsen-Anhalt',SH:'Schleswig-Holstein',TH:'Thüringen'};
const CHECKS=['cRent','cWeg','cTech','cLegal','cMarket','cFinance'];
const ids=['price','area','rent','otherRent','year','type','occupied','address','marketRentM2','marketPriceM2','state','grest','notary','broker','reno','furn','renoTax','nonAlloc','wegReserve','maintM2','admin','vacancy','otherCost','vacOps','taxMaint','ltv','interest','repay','fixed','loanAmount','refiRate','tax','building','afaMode','afa','furnYears','specialAfa','specialAfaYears','lossOffset','horizon','target','rentGrowth','costGrowth','priceGrowth','sellCost','taxFreeSale',...CHECKS];
let step=0, importMeta={sources:[],warnings:[],conflicts:[],confidence:{}};
const n=id=>{const e=$(id); if(!e||e.value==='') return NaN; const x=Number(e.value); return Number.isFinite(x)?x:NaN};
const v=id=>$(id)?.value??'';
const eu=x=>Number.isFinite(x)?EUR.format(x):'–';
const pc=x=>Number.isFinite(x)?NUM.format(x)+' %':'–';
const checked=id=>!!$(id)?.checked;
const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
const val=(id,fallback=0)=>Number.isFinite(n(id))?n(id):fallback;
function defaultAfa(force=false){if(!$('afa'))return;if(!force&&$('afa').value!=='')return;if(v('afaMode')==='declining'){$('afa').value='5';return}const y=n('year');if(!Number.isFinite(y))return;$('afa').value=y>2022?'3':y<1925?'2.5':'2'}
function sync(){if(v('state')&&GR[v('state')]!=null)$('grest').value=GR[v('state')];defaultAfa(false);save()}
function save(){try{const d={};ids.forEach(id=>{const e=$(id);if(!e)return;d[id]=e.type==='checkbox'?e.checked:e.value});localStorage.setItem('iic_v2',JSON.stringify({values:d,meta:importMeta}))}catch(e){}}
function load(){try{let raw=localStorage.getItem('iic_v2');if(!raw){const old=localStorage.getItem('iic');if(old)raw=old}if(raw){const obj=JSON.parse(raw),d=obj.values||obj;ids.forEach(id=>{const e=$(id);if(!e||d[id]==null)return;if(e.type==='checkbox')e.checked=!!d[id];else e.value=d[id]});if(obj.meta)importMeta=obj.meta}}catch(e){}if(v('state')&&GR[v('state')]!=null)$('grest').value=GR[v('state')];defaultAfa(false)}
function show(s){step=s;document.querySelectorAll('.step').forEach(e=>e.classList.toggle('on',+e.dataset.s===s));$('intro').classList.add('hide');$('aiPanel').classList.add('hide');$('prog').classList.remove('hide');$('res').classList.remove('on');$('stxt').textContent=`Schritt ${s} von 7`;$('spct').textContent=Math.round(s/7*100)+' %';$('bar').style.width=(s/7*100)+'%';window.scrollTo({top:0,behavior:'smooth'})}
function valid(s){const sec=document.querySelector(`[data-s="${s}"]`),missing=[];sec.querySelectorAll('[data-r]').forEach(e=>{if(e.value==='')missing.push(e)});const er=$('e'+s);if(er){if(missing.length){er.textContent='Bitte die fehlenden Pflichtangaben ergänzen.';er.classList.add('on');missing[0].focus();return false}er.classList.remove('on')}return true}
function purchase(price=val('price')){const pct=(val('grest')+val('notary')+val('broker'))/100;const closing=price*pct,reno=val('reno'),furn=val('furn');return{closing,all:price+closing+reno+furn,capReno:v('renoTax')==='capitalized'?reno:0,immediateReno:v('renoTax')==='immediate'?reno:0}}
function annualOwnerCosts(vacancy,mult=1){const monthly=(val('nonAlloc')+val('wegReserve')+val('admin'))*12;const normalizedMaint=Math.max(val('maintM2')*val('area'),val('taxMaint'));const vacancyOps=val('vacOps')*12*vacancy;return(monthly+normalizedMaint+val('otherCost')+vacancyOps)*mult}
function annualTaxDeductibleOwnerCosts(vacancy,mult=1){const base=(val('nonAlloc')+val('admin'))*12+val('otherCost')+val('taxMaint')+val('vacOps')*12*vacancy;return base*mult}
function loanInfo(ratePct=val('interest'),price=val('price')){const direct=n('loanAmount');const loan=Number.isFinite(direct)&&direct>0?direct:price*val('ltv')/100;const pay=loan*((ratePct+val('repay'))/100)/12;return{loan,pay}}
function yearLoan(balance,ratePct,pay){let interest=0,principal=0;const i=ratePct/100/12;for(let m=0;m<12&&balance>.01;m++){const im=balance*i;const pm=Math.min(balance,Math.max(0,pay-im));interest+=im;principal+=pm;balance-=pm}return{balance:Math.max(0,balance),interest,principal,debtService:interest+principal}}
function irr(cash){if(!cash.length||cash[0]>=0)return NaN;let lo=-.9999,hi=10;const npv=r=>cash.reduce((s,c,i)=>s+c/Math.pow(1+r,i),0);let a=npv(lo),b=npv(hi);if(!Number.isFinite(a)||!Number.isFinite(b)||a*b>0)return NaN;for(let k=0;k<150;k++){const mid=(lo+hi)/2,x=npv(mid);if(Math.abs(x)<.001)return mid;if(a*x<=0){hi=mid;b=x}else{lo=mid;a=x}}return(lo+hi)/2}
function project(opts={}){const price=Number.isFinite(opts.priceOverride)?opts.priceOverride:val('price');const area=val('area'),totalRentMonthly=Number.isFinite(opts.totalRentMonthly)?opts.totalRentMonthly:(val('rent')+val('otherRent'))*(opts.rentFactor??1);const baseRent=totalRentMonthly*12;const vacancy=clamp((val('vacancy')+(opts.vacancyAdd||0))/100,0,.9);const rentGrowth=(val('rentGrowth')+(opts.rentGrowthDelta||0))/100;const costGrowth=val('costGrowth')/100;const priceGrowth=Number.isFinite(opts.priceGrowthOverride)?opts.priceGrowthOverride/100:(val('priceGrowth')+(opts.priceGrowthDelta||0))/100;const H=val('horizon'),tax=val('tax')/100,buildShare=val('building')/100,afaRate=val('afa')/100;const acq=purchase(price),initialRate=val('interest')+(opts.initialRateAdd||0),fixed=Math.max(1,val('fixed',10));const baseRefi=Number.isFinite(n('refiRate'))?n('refiRate'):val('interest');const refiRate=baseRefi+(opts.refiRateAdd||0);const li=loanInfo(initialRate,price);let balance=li.loan,payment=li.pay,refiPayment=null;const equity=acq.all-li.loan,cash=[-equity],rows=[];const depBase=(price+acq.closing)*buildShare+acq.capReno;let buildingBook=depBase,furnBook=val('furn'),cumBuildingDep=0;const furnYears=Math.max(1,val('furnYears',10)),specialYears=Math.max(0,val('specialAfaYears',4)),specialAnnual=val('specialAfa');for(let y=1;y<=H;y++){const gross=baseRent*Math.pow(1+rentGrowth,y-1),eff=gross*(1-vacancy);const costs=annualOwnerCosts(vacancy,opts.costFactor||1)*Math.pow(1+costGrowth,y-1);let rate=initialRate;if(y>fixed){rate=refiRate;if(refiPayment===null)refiPayment=balance*((refiRate+val('repay'))/100)/12;payment=refiPayment}const L=yearLoan(balance,rate,payment);balance=L.balance;let normalDep=0;if(buildingBook>0){normalDep=v('afaMode')==='declining'?buildingBook*afaRate:depBase*afaRate;normalDep=Math.min(buildingBook,Math.max(0,normalDep))}let specialDep=y<=specialYears?Math.min(Math.max(0,buildingBook-normalDep),specialAnnual):0;const buildingDep=normalDep+specialDep;buildingBook=Math.max(0,buildingBook-buildingDep);cumBuildingDep+=buildingDep;const furnDep=furnBook>0?Math.min(furnBook,val('furn')/furnYears):0;furnBook=Math.max(0,furnBook-furnDep);let deductible=annualTaxDeductibleOwnerCosts(vacancy,opts.costFactor||1)*Math.pow(1+costGrowth,y-1);if(y===1)deductible+=acq.immediateReno;const taxable=eff-deductible-L.interest-buildingDep-furnDep;let taxCash=0;if(tax>0){if(taxable>=0)taxCash=taxable*tax;else if(v('lossOffset')==='yes')taxCash=taxable*tax}const pre=eff-costs-L.debtService,after=pre-taxCash;rows.push({y,gross,eff,costs,interest:L.interest,principal:L.principal,balance,pre,after,taxable,taxCash,rate,buildingDep,furnDep,deductible});cash.push(after)}const sale=price*Math.pow(1+priceGrowth,H),saleNet=sale*(1-val('sellCost')/100);let saleTax=0,taxableSaleGain=0;if(v('taxFreeSale')==='no'&&tax>0){const adjustedRealEstateBasis=(price+acq.closing+acq.capReno)-cumBuildingDep;taxableSaleGain=Math.max(0,saleNet-adjustedRealEstateBasis);saleTax=taxableSaleGain*tax}const terminal=saleNet-balance-saleTax;cash[H]+=terminal;const rr=irr(cash);return{cash,rows,irr:rr,equity,sale,saleNet,saleTax,taxableSaleGain,terminal,balance,acq,loan:li.loan,cumBuildingDep}}
function firstYear(opts={}){const pr=project(opts),r=pr.rows[0];if(!r)return{after:NaN,noi:NaN,ds:NaN,dscr:NaN,coc:NaN};const noi=r.eff-r.costs,ds=r.interest+r.principal;return{...r,noi,ds,dscr:ds>0?noi/ds:Infinity,coc:pr.equity>0?r.after/pr.equity:NaN,eq:pr.equity,loan:pr.loan}}
function stressProject(){return project({rentFactor:.9,vacancyAdd:5,costFactor:1.2,refiRateAdd:2,priceGrowthDelta:-2})}
function financialScore(){const b=project(),f=firstYear(),s=stressProject(),target=val('target')/100;let score=50;if(Number.isFinite(b.irr))score+=clamp((b.irr-target)*220,-28,28);score+=f.after>=0?10:-clamp(Math.abs(f.after)/1200*3,3,15);if(f.dscr>=1.2)score+=10;else if(f.dscr<1)score-=14;else score-=3;if(Number.isFinite(s.irr)){if(s.irr>=Math.max(0,target-.03))score+=8;else if(s.irr<0)score-=12;else score-=3}if(val('ltv')>90&&!Number.isFinite(n('loanAmount')))score-=5;return Math.round(clamp(score,0,100))}
function dataQuality(){const due=CHECKS.filter(checked).length,sources=Array.isArray(importMeta.sources)?importMeta.sources.length:0;return Math.round(clamp(25+due/6*60+Math.min(15,sources*3),0,100))}
function maxPriceForTarget(){if(Number.isFinite(n('loanAmount'))&&n('loanAmount')>0)return NaN;const target=val('target')/100,current=val('price');const fn=p=>project({priceOverride:p}).irr-target;let lo=Math.max(1000,current*.15),hi=current*4,a=fn(lo),b=fn(hi);if(!Number.isFinite(a)||a<0)return NaN;for(let k=0;k<8&&Number.isFinite(b)&&b>0;k++){hi*=1.6;b=fn(hi)}if(!Number.isFinite(b)||b>0)return NaN;for(let k=0;k<80;k++){let mid=(lo+hi)/2,x=fn(mid);if(!Number.isFinite(x))return NaN;if(x>=0)lo=mid;else hi=mid}return(lo+hi)/2}
function breakEvenRent(){const current=Math.max(1,val('rent')+val('otherRent'));const cf=monthly=>firstYear({totalRentMonthly:monthly}).after;let lo=0,hi=Math.max(current*4,5000),a=cf(lo),b=cf(hi);if(!Number.isFinite(a)||!Number.isFinite(b)||b<0)return NaN;for(let k=0;k<70;k++){let mid=(lo+hi)/2,x=cf(mid);if(x>=0)hi=mid;else lo=mid}return(lo+hi)/2}
function equityMultiple(cash){let invested=Math.max(0,-cash[0]),dist=0;for(let i=1;i<cash.length;i++){if(cash[i]<0)invested+=-cash[i];else dist+=cash[i]}return invested>0?dist/invested:NaN}
function combinedStressRefiCash(s){const idx=Math.min(s.rows.length-1,Math.max(0,Math.round(val('fixed'))));return s.rows[idx]?.after}
function rating(){const b=project(),f=firstYear(),s=stressProject(),target=val('target')/100;const strong=Number.isFinite(b.irr)&&b.irr>=target&&f.after>=0&&f.dscr>=1.2&&Number.isFinite(s.irr)&&s.irr>=0;const acceptable=Number.isFinite(b.irr)&&b.irr>=target-.02&&f.dscr>=1&&Number.isFinite(s.irr)&&s.irr>-.03;if(strong)return['Wirtschaftlich attraktiv – Detailprüfung lohnt sich','Rendite, Liquidität und Kapitaldienst sind unter den eingegebenen Annahmen solide; die Due Diligence bleibt entscheidend.','good'];if(acceptable)return['Prüfenswert – Preis oder Annahmen nachschärfen','Das Investment kann funktionieren, besitzt aber nur begrenzten Sicherheitspuffer.','warn'];return['Unter den Annahmen eher unattraktiv','Rendite, Liquidität oder Risikopuffer reichen derzeit nicht aus.','bad']}
